version: 2.1

orbs:
  slack: circleci/slack@3.4.2

executors:
  terraform:
    working_directory: ~/project
    docker:
      - image: hashicorp/terraform:light

commands:
  terraform_init:
    steps:
      - run:
          command: apk update; apk add bash
      - run:
          name: terraform init
          command: terraform init ~/project

jobs:
  test:
    executor:
      name: terraform
    steps:
      - checkout
      - terraform_init
      - run:
          name: terraform plan
          command: terraform plan ~/project
      - slack/status:
          fail_only: true
          failure_message: ":circleci-fail: テストが失敗しました\n:github_octocat: User:$CIRCLE_USERNAME"
          webhook: "${SLACK_WEBHOOK}"

  deploy:
    executor:
      name: terraform
    steps:
      - checkout
      - terraform_init
      - run:
          name: terraform apply
          command: terraform apply -auto-approve ~/project
      - slack/status:
          channel: "#sns_test"
          success_message: ":circleci-pass: デプロイが完了しました\n:github_octocat: User:$CIRCLE_USERNAME"
          failure_message: ":circleci-fail: デプロイが失敗しました\n:github_octocat: User:$CIRCLE_USERNAME"
          webhook: "${SLACK_WEBHOOK}"

workflows:
  test_and_deploy:
    jobs:
      - test:
          context: aws-settings
      - hold:
          type: approval
          requires:
            - test
          filters:
            branches:
              only: master
      - deploy:
          requires:
            - hold
          filters:
            branches:
              only: master
          context: aws-settings
